# Windows onedir freeze only (play/render). Do not pip install -r requirements.txt
# in the freeze venv; that pulls torch, demucs, librosa, and analyse deps.
# See docs/windows-freeze.md and packaging/windows/README.md.

pyinstaller==6.22.2
pygame==2.6.1
PyYAML==6.0.3
numpy==2.2.6
soxr==1.1.0
moderngl==5.12.0
PyOpenGL==3.1.10
soundfile==0.13.1
